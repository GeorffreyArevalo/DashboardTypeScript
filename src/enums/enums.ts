export enum Status {
    Same = "same",
}

export enum Name {
    PremierLeague = "Premier League",
}

export enum Position {
    Attacker = "Attacker",
    Midfielder = "Midfielder",
}


export enum Country {
    England = "England",
}
